function refreshOpenEye(newLine) {
  const wordLine = document.querySelector('[data-word-id="<%= @word.id %>"]');
  const hideShow = wordLine.querySelector(".hide-show");
  hideShow.outerHTML = newLine
}

refreshOpenEye("<%= j render 'shared/new-line', word: @word, list: @list, index: "index" %>")


